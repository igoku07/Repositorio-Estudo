#include <stdio.h>


typedef struct{
 char nMat[10];
 char nome[81];
 char email[41];
}Aluno;


main()
{

//FORMULA PARA HASHEAR MEU RA, 

//hash(key) = (key % 1000) % 31
 
// a ideia da conta (361835 % 1000) % 31

hash(key) = (key % 1000) % 31

hash(key) = 29

//e se dois alunos tiverem o memso hash code? VC DEVE FAZER TRATAMENTO DE COLISOES

// * TENTATIVA DE COLOCAR EM ALGUM LUGAR QUE TA DISPONIVEL
// * lista ligada e tem outro


}